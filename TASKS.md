# ownkey 开发任务清单

标记说明：✅ 已完成 · 🟡 进行中/待开始 · 🔜 下一步

## 已完成
- ✅ CLI 框架与命令路由（clap）：`init`/`add`/`list`/`view`/`delete`/`search` 结构就绪
- ✅ Vault 持久化基础：`ensure_vault_exists` 创建空 JSON，`load_vault` / `save_vault` 使用 `serde_json`
- ✅ 命令实现（KV 版占位逻辑）：`init`/`add`/`list`/`view`/`delete`/`search` 工作并通过集成测试
- ✅ 测试脚手架：`tests/cli_smoke.rs` 运行通过；`tests/vault_flow.rs` 覆盖 KV 模式 init/add/list/view/delete/search 流程
- ✅ README 与项目结构/依赖配置

## 待实现（建议顺序）
- 🟡 交互输入与验证：为 `add` 等命令添加用户输入提示
- 🟡 安全/后续：加密、权限强化、备份策略、友好输出格式

## TDD 提示
- 当前集成测试全绿；后续为新功能补充测试再实现。
